_target_: context_flux_no.models.multiphysics.DPOT
num_spatial_dims: ${..data.num_spatial_dims}
in_channels: ${..data.channels}
out_channels: ${..data.channels}
in_timesteps: ${..training.context_length}
patch_size: [4,]
img_size: [100,]
embedding_dim: 256
max_frequency_modes: [10,]
fno_depth: 5
num_blocks: 4
num_classes: 12
hidden_dim_patch: ${.embedding_dim}
hidden_dim_fno: ${.embedding_dim}
hidden_dim_output: 32
activation:
  _target_: hydra.utils.get_method
  path: jax.nn.gelu
key:
  _target_: jax.random.key
  seed: 0